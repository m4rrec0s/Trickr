import Calendar from '@/components/calendar';
import { View } from 'react-native';

const Home = () => {

  return (
    <View className="flex-1">
      <Calendar />
    </View>
  );
};

export default Home;