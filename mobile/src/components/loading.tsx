import { ActivityIndicator } from "react-native";

const Loading = () => {
  return (
    <ActivityIndicator className="flex-1 bg-white items-center justify-center text-gray-900" />
  );
};

export default Loading;
